import PicGo from '../core/PicGo';
import { Result, ProcessEnv } from '../utils/interfaces';
declare class PluginHandler {
    ctx: PicGo;
    constructor(ctx: PicGo);
    install(plugins: string[], proxy?: string, env?: ProcessEnv): Promise<void>;
    uninstall(plugins: string[]): Promise<void>;
    update(plugins: string[], proxy?: string, env?: ProcessEnv): Promise<void>;
    execCommand(cmd: string, modules: string[], where: string, proxy?: string, env?: ProcessEnv): Promise<Result>;
}
export default PluginHandler;
